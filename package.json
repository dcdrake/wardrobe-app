{
  "name": "wardrobe-stylist",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev:*\"",
    "dev:backend": "cd apps/backend && uv run python manage.py runserver",
    "dev:frontend": "cd apps/frontend && npm run dev",
    "build": "cd apps/frontend && npm run build",
    "lint": "cd apps/frontend && npm run lint",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "migrate": "cd apps/backend && uv run python manage.py migrate",
    "setup": "npm install && cd apps/backend && uv sync && cp -n .env.example .env || true && cd ../.. && npm run db:up && sleep 2 && npm run migrate"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
